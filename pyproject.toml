[project]
name = "liquidity-pools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "authx>=1.6.0",
    "bcrypt==4.0.1",
    "fastapi[standard,standart]>=0.136.1",
    "scalar-fastapi>=1.8.2",
    "web3>=7.16.0",
]
