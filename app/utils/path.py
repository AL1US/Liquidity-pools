from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent

STATIC_PATH = PROJECT_ROOT / "app" / "frontend" / "static"
 
TEMPLATE_PATH = PROJECT_ROOT / "app" / "frontend" / "templates"